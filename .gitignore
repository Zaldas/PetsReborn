# Local editor config (machine-specific)
.vscode/

# asimov knowledge base (lives in the private addons monorepo)
knowledge

# Internal design notes and working docs — never tracked, never shipped
.notes/

# Release archives and build output
release/
*.zip
*.rar
